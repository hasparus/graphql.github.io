// Stub data file for GraphQL Day schedule.
// Will be populated when talks are announced.
import type { SchedSpeaker, ScheduleSession } from "@/app/conf/_api/sched-types"

export const schedule: ScheduleSession[] = []
export const speakers: SchedSpeaker[] = []
